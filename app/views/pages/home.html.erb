<h1>Anyone can see this page(logged in or not!)</h1>
<% if user_signed_in? && current_user.id != nil %>
     <ul>
          <% @posts.each do |post| %>
               <li><%= render partial: "posts/shared/meme_post", locals: { post: post } %></li>
               <%= render partial: "posts/shared/edit_or_delete", locals: { post: post } %>
          <% end %>
     </ul>
<% else %>
     <%= render "pages/shared/login_or_signup" %>
<% end %>